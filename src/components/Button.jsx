import React from 'react';

const Button = ({ data }) => {
   
   
   return (
      <div>
         <h1 className='px-4 py-1 border bg-gray-300 rounded-lg'>{data}</h1>
      </div>
   );
};

export default Button;
